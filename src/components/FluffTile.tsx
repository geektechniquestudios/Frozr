interface Props {}

export const FluffTile: React.FC<Props> = ({}) => {
  return (
    <div
      className="h-[32em] shrink-0 rounded-md  border border-stone-600 bg-slate-400 bg-opacity-20"
      style={{
        backdropFilter: "blur(16px)",
      }}
    >

      {/* I'll pitch stuff here
      -decentralized
      -free
      -open source
      -no ads
      -no tracking
      -no data collection
      -view contract on blockchain */}

    </div>
  )
}
